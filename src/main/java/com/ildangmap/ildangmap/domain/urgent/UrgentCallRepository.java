package com.ildangmap.ildangmap.domain.urgent;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UrgentCallRepository
        extends JpaRepository<UrgentCall, Long> {
}